pwd                     # đang ở đâu
ls -la                  # xem file + thư mục + ẩn + chi tiết
cd ~                    # về home
cd ..                   # lên 1 cấp
cd -                    # quay lại chỗ cũ
mkdir -p a/b/c          # tạo thư mục kể cả cấp cha
touch file.txt          # tạo file rỗng
rm file.txt             # xóa file
rm -rf folder/          # xóa thư mục (cẩn thận)
cp -r src/ dst/         # copy thư mục
mv old new              # đổi tên hoặc di chuyển
find . | sort           # xem toàn bộ cấu trúc